const collection = document.querySelector('.dropbtn');
collection.addEventListener('click', (event) => {
  const droplist = document.querySelector(".dropdown-content-left");
  droplist.style.display = "";
});
